<div class="card">
    <div class="flag-img">
        <img src="{{flag}}" alt="flag {{name}} ">
    </div>
    <h1>{{name}}</h1>
    <ul>
        <li>capital: {{capital}}</li>
        <li>population: {{population}}</li>
        <li>language:
            <ul>{{#each languages}}
                <li>{{name}}</li>
                {{/each}}
            </ul>
        </li>
    </ul>
</div>