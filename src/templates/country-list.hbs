<div class="list-countries">
    <ul>{{#each this}}
        <li>
            <div class="flag-img">
                <img src="{{flag}}" alt="flag {{name}}" height="20px">
            </div>
            {{name}}
        </li>
        {{/each}}
    </ul>
</div>